using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Statusbarra : MonoBehaviour
{/*
    // Start is called before the first frame update
    void Start(){
        
    }

    // Update is called once per frame
    void Update(){
        
    }

    public float PegarTamanhoBarra(float _minValor, float _maxValor, int _fator) { 
    return _minValor / _maxValor;
    }

    public int PegarTamanhoBarra2(float _minValor, float _maxValor, int _fator) {
        return Mathf.RoundToInt( PegarTamanhoBarra(_minValor, _maxValor) * _fator );
    }*/
}
